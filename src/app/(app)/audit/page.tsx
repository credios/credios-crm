import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function AuditPage() {
  return (
    <PagePlaceholder
      title="Auditoria"
      description="Trilha de eventos do sistema (LGPD)"
      fase="Fase 4 (administração)"
    />
  );
}
