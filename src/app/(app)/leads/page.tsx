import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function LeadsPage() {
  return (
    <PagePlaceholder
      title="Leads"
      description="Lista, filtros e busca de leads"
      fase="Fase 3 (pipeline)"
    />
  );
}
